// Generated by CoffeeScript 1.7.1
(function() {
  var Language, format, formatUnsafe, lts, moment, prop, _fn,
    __slice = [].slice;

  moment = require("lib/moment");

  Language = {};

  format = function() {
    var args;
    args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
    return formatUnsafe.apply(null, args);
  };

  lts = {
    fullDateFormatString: "dddd, MMMM Do YYYY",
    thisManyPeopleHasShareIt_i: "%s people shares it",
    andThisMorePeopleHasShareIt_i: "and %s people shares it",
    sharesIt: "shares it"
  };

  _fn = function(prop) {
    return Language[prop] = function() {
      var args;
      args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
      return format.apply(this, [].concat(lts[prop], args));
    };
  };
  for (prop in lts) {
    _fn(prop);
  }

  moment.lang(navigator.language);

  format = function() {
    var args;
    args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
    return formatUnsafe.apply(null, args);
  };

  formatUnsafe = function() {
    var args, text, value, _i, _len;
    args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
    text = args.shift();
    for (_i = 0, _len = args.length; _i < _len; _i++) {
      value = args[_i];
      text = text.replace("%s", value);
    }
    return text;
  };

  module.exports = Language;

}).call(this);
