// Generated by CoffeeScript 1.7.1
(function() {
  var TextGhost;

  TextGhost = (function() {
    function TextGhost(target, text) {
      this.setTarget(target);
      this.setText(text);
      return;
    }

    TextGhost.prototype.setTarget = function(target) {
      this.target = target;
      this.ghost = this.target.cloneNode(false);
      return this.ghost.style = this.target.style;
    };

    TextGhost.prototype.setText = function(text) {
      return this.text;
    };

    TextGhost.prototype.getLayoutInfo = function() {
      var height, lineHeight, overhead;
      height = this.ghost.style.height;
      this.ghost.style.height = "auto";
      overhead = $(this.ghost).height();
      this.ghost.innerHTML = "A";
      lineHeight = $(this.target).height() - overhead;
      this.ghost.style.height = height;
      return {
        overhead: overhead,
        lineHeight: lineHeight
      };
    };

    TextGhost.prototype.getTextByLine = function(text) {
      var count, info, max, min;
      text = text || this.text || "";
      count = text.length;
      min = 0;
      max = count;
      return info = this.getLayoutInfo();
    };

    TextGhost.prototype.fit = function() {};

    TextGhost.prototype.render = function(text) {
      return info;
    };

    return TextGhost;

  })();

}).call(this);
