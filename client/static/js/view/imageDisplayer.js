// Generated by CoffeeScript 1.8.0
(function() {
  var App, CubeLoadingHint, ImageDisplayer, Popup, SmartImage, tm,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  App = require("/app");

  tm = require("/templateManager");

  SmartImage = require("/widget/smartImage");

  Popup = require("/widget/popup");

  CubeLoadingHint = require("/widget/cubeLoadingHint");

  tm.use("imageDisplayer");

  ImageDisplayer = (function(_super) {
    __extends(ImageDisplayer, _super);

    function ImageDisplayer() {
      this.include(SmartImage);
      this.include(CubeLoadingHint);
      ImageDisplayer.__super__.constructor.call(this, App.templates.imageDisplayer);
      this.UI.image.on("state", (function(_this) {
        return function(state) {
          console.debug("stae", state, "...");
          if (state === "loading") {
            return _this.VM.imageState = "loading";
          } else {
            return _this.VM.imageState = "ready";
          }
        };
      })(this));
      this.UI.image.onclick = (function(_this) {
        return function() {
          return _this.hide();
        };
      })(this);
    }

    ImageDisplayer.prototype.setSrc = function(src) {
      return this.UI.image.src = src;
    };

    ImageDisplayer.prototype.onClickNode = function() {
      return this.hide();
    };

    ImageDisplayer.prototype.onClickImage = function() {
      return this.hide();
    };

    return ImageDisplayer;

  })(Popup);

  module.exports = ImageDisplayer;

}).call(this);
