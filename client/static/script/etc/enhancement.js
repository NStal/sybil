// Generated by CoffeeScript 1.8.0
(function() {
  var App;

  App = require("/app");

  App.on("structureReady", function() {
    var e, lastSceneName;
    lastSceneName = App.userConfig.get("lastScene") || "source scene";
    App.sceneSwitcher.on("sceneChange", function(scene) {
      return App.userConfig.set("lastScene", scene.name);
    });
    try {
      return App.sceneSwitcher.switchTo(lastSceneName);
    } catch (_error) {
      e = _error;
      return App.sceneSwitcher.switchTo("source scene");
    }
  });

}).call(this);
